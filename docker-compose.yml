version: "3.8"

services:
  redis:
    image: redis:latest
    container_name: redis-travel
    ports:
      - "6379:6379"
    networks:
      - travel-network
    volumes:
      - ./redis-data:/data



networks:
  travel-network:
    name: travel-network
